// Sidebar-specific enums and types

export enum PageType {
  HOME = "home",
  PROFILE = "profile",
  SETTINGS = "settings",
  SNIPPETS = "snippets",
}

export enum ConfirmDialogVariant {
  DEFAULT = "default",
  DANGER = "danger",
  WARNING = "warning",
}
