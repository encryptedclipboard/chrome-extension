export enum SubscriptionPlanType {
  FREE = "free",
  BASIC = "basic",
  PREMIUM = "premium",
  PREMIUM_PLUS = "premium+",
}

export enum PlanDuration {
  MONTHLY = "monthly",
  YEARLY = "yearly",
  LIFETIME = "lifetime",
}
