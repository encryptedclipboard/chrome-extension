export enum PaymentProvider {
  DODO = "dodo",
  STRIPE = "stripe",
  PAYPAL = "paypal",
  BKASH = "bkash",
}

export enum EmailProvider {
  SENDGRID = "sendgrid",
  MAILGUN = "mailgun",
  SES = "ses",
  CUSTOM = "custom",
}

export enum Currency {
  USD = "USD",
  EUR = "EUR",
  GBP = "GBP",
  BDT = "BDT",
  INR = "INR",
}
